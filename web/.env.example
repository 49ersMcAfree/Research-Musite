# If you want the frontend to call Matomo directly from the browser, set these.
# WARNING: embedding token_auth in a client-side .env exposes the token to anyone
# who can view your built javascript. Prefer a read-only Matomo token scoped to
# the site and only grant view access. Do NOT commit real tokens.
REACT_APP_MATOMO_URL=http://localhost:8080
REACT_APP_MATOMO_TOKEN=REPLACE_WITH_READONLY_TOKEN

# If these are not set, the app will attempt to reach /matomo/* on the same-origin
# then fall back to http://localhost:3344 (legacy proxy).
